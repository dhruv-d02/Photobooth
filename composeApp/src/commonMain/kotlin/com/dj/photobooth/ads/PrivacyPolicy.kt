package com.dj.photobooth.ads

// TODO(admob-launch): point this at the real hosted privacy policy before shipping ads to real
// users - required by both Play Store policy and AdMob program policy once ad serving is live
// (see architecture.md's AdMob section). Deliberately an obvious placeholder rather than a
// real-looking-but-wrong URL, so a forgotten swap is loud instead of silently broken.
const val PRIVACY_POLICY_URL = "https://example.com/boothie-privacy-policy"
