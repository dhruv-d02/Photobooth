# Add project specific ProGuard rules here.
# By default, the flags in this file are appended to flags specified
# in D:\GitHub\PhotoBooth\composeApp\build.gradle.kts (getDefaultProguardFile).
#
# Currently unused - isMinifyEnabled is false (see build.gradle.kts). This file exists so
# Phase 5 (store prep) can turn minification on without also having to remember to
# create a rules file at the same time; add real keep-rules here once R8 is actually
# running and something needs protecting from being stripped/renamed (e.g. Compose
# runtime reflection, kotlinx.serialization models if/when those are introduced).
